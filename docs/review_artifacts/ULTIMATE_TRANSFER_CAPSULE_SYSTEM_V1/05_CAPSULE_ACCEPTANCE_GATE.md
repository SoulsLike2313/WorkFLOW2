# 05_CAPSULE_ACCEPTANCE_GATE

Seed capsule is `SEED_READY` only if all checks pass.

1. `for_chatgpt/01` and `for_chatgpt/02` are directly paste-ready
2. GPT can reconstruct:
- IMPERIUM identity
- immutable canon
- mutable state
- line split
- latest major steps
- current active vertex
- what are we doing
- next bounded step
- open gaps
- roles/laws
3. Codex onboarding is sufficient for bounded reconstruction and execution limits
4. current review injection path is explicit (`09` + `for_chatgpt/06` + `for_codex/05`)
5. foundation refresh path is explicit and Emperor-controlled
6. mutable refresh path is explicit and routine
7. stale active-live wording is absent
8. one clean artifact entrypoint path is emitted externally
9. seed works for local/current/new machine contexts
10. no fake completeness claims
11. continuity/handoff/live lines remain separate
12. execution-node companion branch is clearly marked as non-main restore branch
