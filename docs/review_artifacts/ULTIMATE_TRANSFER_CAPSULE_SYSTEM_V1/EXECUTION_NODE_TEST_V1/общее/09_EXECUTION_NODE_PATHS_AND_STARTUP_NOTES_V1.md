# EXECUTION NODE PATHS AND STARTUP NOTES V1

Рекомендуемая стартовая привязка на ноутбуке:

1. локальный рабочий repo path:
- `<laptop_repo_root>`

2. capsule addendum source:
- `docs/review_artifacts/ULTIMATE_TRANSFER_CAPSULE_SYSTEM_V1/EXECUTION_NODE_TEST_V1/`

3. базовый протокол запуска:
- GPT entry -> Codex entry -> owner yes -> fixation prompt.

4. стартовый runtime-check:
- доступность python/git;
- доступность локального dashboard/server endpoint (если используется);
- запись артефактов в `docs/review_artifacts/`.

5. перед return bundle:
- проверить, что отчеты не заявляют фейковую полноту;
- явно отметить unknown и owner decision points.
