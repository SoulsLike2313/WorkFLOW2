# FEDERATION_OPERATIONAL_MODEL

Status:
- model_version: `v1-current`
- scope: `bounded doctrinal correction for current Federation operation`
- non_goal: `no broad redesign and no rank-model rewrite`

## 1) Current Operational Truth

1. Federation is the operational development block only.
2. Federation is not the sovereign core and cannot override sovereign gates.
3. Current stage has exactly one real department:
   - `Analytics Department`
   - current implementation path: `projects/platform_test_agent`
4. Transitional naming is allowed:
   - `platform_test_agent (current implementation of Analytics Department)`.

## 2) Analytics Department Mission

`Analytics Department` performs:
1. intake of applications/files/messages about application creation;
2. primary parsing and audit;
3. determination of current state;
4. identification of done/not-done scope;
5. initial planning;
6. primary recommendations for next execution blocks.

Detailed doctrine:
1. `docs/governance/ANALYTICS_DEPARTMENT_DOCTRINE.md`
2. `docs/governance/TEST_PRODUCT_INTAKE_MODEL.md`

## 3) Non-Department Operational Lines (Current Stage)

The following lines are not departments in current operational framing:

| line | path | current framing |
|---|---|---|
| `tiktok_agent_platform` | `projects/wild_hunt_command_citadel/tiktok_agent_platform` | `test_product` + `intake_subject` + `analysis_candidate` |
| `game_ru_ai` | `projects/GameRuAI` | `test_product` + `intake_subject` + `analysis_candidate` |
| `voice_launcher` | `projects/voice_launcher` | `test_product` + `intake_subject` + `analysis_candidate` |
| `adaptive_trading` | `projects/adaptive_trading` | `test_product` + `intake_subject` + `analysis_candidate` |

## 4) Historical Trace Downgrade

Not current operational entities:
1. `shortform_core` (historical residue only where referenced for migration history).
2. `tiktok_automation_app` (historical migration trace only).
3. `wild_hunt_command_citadel` (container path, not a department).

## 5) Command Boundary in This Model

1. Sovereign layer remains separate from Federation.
2. Department command architecture in current stage is bounded to one department only.
3. Any future multi-department formalization requires explicit new doctrine and registry updates.

## 6) Load-Bearing References

1. `docs/governance/FEDERATION_ARCHITECTURE.md`
2. `docs/CURRENT_PLATFORM_STATE.md`
3. `workspace_config/workspace_manifest.json`
4. `docs/governance/FEDERATION_DEPARTMENT_ESCALATION_MATRIX.md`
5. `docs/governance/PRIMARCH_ASTARTES_DELEGATION_MATRIX.md`
6. `docs/governance/ANALYTICS_DEPARTMENT_DOCTRINE.md`
7. `docs/governance/TEST_PRODUCT_INTAKE_MODEL.md`
8. `workspace_config/test_product_intake_contract.json`
