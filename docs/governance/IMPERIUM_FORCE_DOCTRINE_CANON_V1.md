# IMPERIUM_FORCE_DOCTRINE_CANON_V1

Status:
- class: `foundation_operational_doctrine`
- mutability: `immutable_by_default`
- change_authority: `EMPEROR_ONLY`
- implementation_state: `active_surface_with_runtime_metrics`

Core law:
- strength does not come from nowhere.
- every major mission must expose force profile before execution.

Force dimensions:
1. `compute_power` (CPU/RAM/storage IO/GPU/network profile),
2. `tool_power` (git/runtime/validation/export/tooling availability),
3. `knowledge_power` (repo truth + bundle truth + doctrine reach),
4. `authority_power` (read/write/clean/quarantine/fix/lock/attest/escalate),
5. `clarity_power` (intent/scope/unknown/assumption pressure),
6. `execution_power` (architecture readiness/code health/runtime feasibility),
7. `validation_power` (proof/test/smoke/evidence ability),
8. `coordination_power` (handoff/node split readiness).

Mandatory force surfaces:
1. machine capability manifest,
2. organ strength surface (Custodes/Inquisition/Mechanicus/Administratum),
3. mission cost model for major tasks,
4. bottleneck and saturation disclosure.

Boundary:
1. no fake high-capacity claims,
2. no fake node-readiness claims,
3. no fake validation claims,
4. no bypass of owner checkpoint when force is insufficient.
