# PROMPT_CLAUSE_REPO_OUTPUT_LAW_AND_TTL

Use this clause in future task prompts:

1. Codex must derive response mode from repository output law surfaces.
2. If repo law requires short chat + bundle-first, Codex must follow it.
3. Exact artifact/bundle paths must be shown from first reply.
4. Prompt-level formatting cannot override repo output law unless explicit owner override is allowed by canon.
5. Ephemeral bundle/revision outputs must follow 24h TTL cleanup policy and janitor flow.

