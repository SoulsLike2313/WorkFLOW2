# DIRECTED_EVOLUTION_CLUSTER_REGISTRY_V1

Status:
- registry_state: `ACTIVE`
- model: `clustered_evolution`
- owner_control_mode: `MANDATORY`

## Cluster Set

### C1 — Truth / Verification / Evidence

- cluster_id: `c1_truth_verification_evidence`
- purpose: keep truth quality, evidence density, and verification integrity non-negotiable.
- allowed growth directions:
  1. stronger validation depth,
  2. tighter proof-path discipline,
  3. contradiction closure tooling.
- forbidden drift directions:
  1. decorative reporting without evidence gain,
  2. replacing proofs with narrative confidence.
- maturity ladder: `SEED -> ACTIVE -> HARDENED -> CERTIFIED`.
- promotion conditions:
  1. measurable verification gain,
  2. no increase in false-pass risk.
- rollback/containment:
  1. revert candidate if proof quality drops or ambiguity rises.

### C2 — Product Hardening / Reliability

- cluster_id: `c2_product_hardening_reliability`
- purpose: convert product lanes from fragile to reliable release-grade shape.
- allowed growth directions:
  1. must-fix foundations,
  2. reliability hardening,
  3. release-shape integrity.
- forbidden drift directions:
  1. premature feature sprawl,
  2. polish-first while foundation remains red.
- maturity ladder: `INTAKE -> FOUNDATION -> STABILIZATION -> RELEASE_READY`.
- promotion conditions:
  1. risk board trend improves,
  2. verification alignment is green for required gates.
- rollback/containment:
  1. hold promotion and revert to previous stable tranche when trust degrades.

### C3 — Owner Observation / Presence UX

- cluster_id: `c3_owner_observation_presence_ux`
- purpose: improve owner visibility/clarity without distorting truth.
- allowed growth directions:
  1. readability, clarity, trust polish,
  2. better owner-facing summaries with traceability.
- forbidden drift directions:
  1. fake realtime,
  2. decorative masking of missing data.
- maturity ladder: `SCAFFOLD -> READABLE -> TRUSTED -> OPERATOR_GRADE`.
- promotion conditions:
  1. no truth loss,
  2. no command-mode regression,
  3. visual evidence confirms readability gain.
- rollback/containment:
  1. disable/rollback presentation layer changes that reduce trust.

### C4 — Growth / Distribution / Monetization

- cluster_id: `c4_growth_distribution_monetization`
- purpose: controlled market-entry growth connected to product reality.
- allowed growth directions:
  1. distribution seeding,
  2. early-sales readiness artifacts,
  3. narrative/channel starter packs.
- forbidden drift directions:
  1. growth claims before hardening baseline,
  2. marketing inflation unsupported by product truth.
- maturity ladder: `SEED -> PILOT -> EARLY_SIGNAL -> SYSTEMIZED`.
- promotion conditions:
  1. product hardening prerequisites met,
  2. owner-approved positioning.
- rollback/containment:
  1. freeze growth branch if hardening baseline regresses.

### C5 — Factory Governance / Memory / Character

- cluster_id: `c5_factory_governance_memory_character`
- purpose: protect coherent governance memory and controlled character-layer evolution.
- allowed growth directions:
  1. law/ledger hardening,
  2. anti-drift protocol refinement,
  3. owner-controlled memory indexing.
- forbidden drift directions:
  1. hidden personality mutation,
  2. character-layer override of governance law.
- maturity ladder: `BASELINE -> ENFORCED -> INTEGRATED -> AUDIT_STABLE`.
- promotion conditions:
  1. explicit owner acceptance for kernel-level changes,
  2. no conflict with governance hierarchy.
- rollback/containment:
  1. reject and revert candidate if hidden-learning pattern appears.

### C6 — Visual Language / Theme / Interface Maturity

- cluster_id: `c6_visual_language_theme_interface_maturity`
- purpose: mature visual language without sacrificing hierarchy/traceability.
- allowed growth directions:
  1. token-system refinement,
  2. readability-first style improvements,
  3. role-aware screen archetype maturation.
- forbidden drift directions:
  1. visual spectacle over trust,
  2. style overrides that break traceability.
- maturity ladder: `STYLE_BASE -> STRUCTURED -> CONSISTENT -> PRODUCTION_UI`.
- promotion conditions:
  1. truth labels remain explicit,
  2. no data-class ambiguity introduced.
- rollback/containment:
  1. revert style changes that reduce information hierarchy clarity.

### C7 — Interactive / Campaign Future Modes

- cluster_id: `c7_interactive_campaign_future_modes`
- purpose: explore future interactive/campaign modes under strict containment.
- allowed growth directions:
  1. bounded prototypes,
  2. optional campaign mode experiments tied to existing truth data.
- forbidden drift directions:
  1. gamified misrepresentation of system state,
  2. overriding canonical control surfaces.
- maturity ladder: `FROZEN -> PROTOTYPE -> GUARDED_PILOT -> ADMITTED_MODE`.
- promotion conditions:
  1. explicit owner gate,
  2. boundary compliance with C1/C5.
- rollback/containment:
  1. immediate freeze if mode introduces trust distortion.

## Non-Overlap Rule

Each candidate must declare one primary cluster.
Secondary cluster linkage is optional, but cannot transfer authority across boundaries.

